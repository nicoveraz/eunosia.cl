# eunosia.cl

Landing page for Eunosia — a clinical AI platform for emergency medicine.

## Stack

HTML + CSS + JS vanilla, single file. No build step, no dependencies.

## Development

Open `index.html` in a browser. That's it.

## Deployment

GitHub Pages on the `main` branch with a custom domain via `CNAME`.
